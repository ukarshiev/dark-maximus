{
  "name": "shared-utils",
  "root": "libs/shared-utils",
  "sourceRoot": "libs/shared-utils/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Shared utils library - no build needed'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python -m pytest libs/shared-utils/tests/",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": ["scope:shared", "type:lib"]
}
