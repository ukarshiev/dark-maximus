{
  "name": "shared-utils",
  "root": "libs/shared-utils",
  "sourceRoot": "libs/shared-utils/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Shared utils library - no build needed'"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python -c \"import pathlib, sys; path = pathlib.Path('libs/shared-utils/tests'); sys.exit(__import__('pytest').main([str(path)])) if path.exists() else print('No tests for shared-utils; skipping')\"",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": ["scope:shared", "type:lib"]
}
