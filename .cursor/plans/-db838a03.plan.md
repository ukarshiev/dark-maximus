<!-- db838a03-e728-4b12-ad9d-593005e40973 04bd267b-8a59-4964-8c93-eb00ef0b7e97 -->
# Скрытие кнопки "Инструкции" при отключенных инструкциях

## Цель

Если в панели в разделе "Инструкции" все инструкции отключены (все платформы и видеоинструкции), то в боте скрывается пункт "🌐 Инструкции❓".

## Изменения

### 1. Добавить функцию проверки наличия включенных инструкций

**Файл:** `src/shop_bot/data_manager/database.py`

Добавить функцию `has_any_instructions_enabled() -> bool`, которая проверяет:

- Есть ли хотя бы одна платформа с `show_in_bot = true` (android, ios, windows, macos, linux)
- Или включены ли видеоинструкции (`video_instructions_show_in_bot = true`)

Функция должна возвращать `True`, если хотя бы одна инструкция включена, и `False`, если все отключены.

**Расположение:** После функции `set_video_instructions_display_setting()` (около строки 9534)

### 2. Обновить функцию создания клавиатуры центра помощи

**Файл:** `src/shop_bot/bot/keyboards.py`

В функции `create_help_center_keyboard()` (строка 188):

- Импортировать функцию `has_any_instructions_enabled` из `shop_bot.data_manager.database`
- Добавить кнопку "🌐 Инструкции❓" только если `has_any_instructions_enabled()` возвращает `True`

**Изменение:** Строка 196 - обернуть в условие:

```python
if has_any_instructions_enabled():
    builder.button(text="🌐 Инструкции❓", callback_data="howto_vless")
```

### 3. Обновить функцию создания клавиатуры QR-кода

**Файл:** `src/shop_bot/bot/keyboards.py`

В функции `create_qr_keyboard()` (строка 617):

- Импортировать функцию `has_any_instructions_enabled` из `shop_bot.data_manager.database`
- Добавить кнопку "🌐 Инструкции❓" только если `has_any_instructions_enabled()` возвращает `True`

**Изменение:** Строка 621 - обернуть в условие:

```python
if has_any_instructions_enabled():
    builder.button(text="🌐 Инструкции❓", callback_data=f"howto_vless_{key_id}")
```

## Логика проверки

Функция `has_any_instructions_enabled()` проверяет:

1. Текстовые инструкции для платформ: android, ios, windows, macos, linux
2. Видеоинструкции: `video_instructions_show_in_bot`

Если хотя бы одна инструкция включена (любая платформа или видеоинструкции), функция возвращает `True`.

Если все инструкции отключены, функция возвращает `False`.

## Тестирование

После реализации проверить:

1. Когда все инструкции отключены - кнопка "Инструкции" не отображается
2. Когда хотя бы одна инструкция включена - кнопка "Инструкции" отображается
3. Проверка работает в центре помощи (`create_help_center_keyboard`)
4. Проверка работает в меню QR-кода (`create_qr_keyboard`)

### To-dos

- [ ] Добавить функцию has_any_instructions_enabled() в database.py для проверки наличия включенных инструкций
- [ ] Обновить create_help_center_keyboard() для условного отображения кнопки 'Инструкции'
- [ ] Обновить create_qr_keyboard() для условного отображения кнопки 'Инструкции'