<!-- 99549cdd-999f-41c9-adb4-6be7a081e207 2f5a547f-b411-4fe0-8d6f-57bf5369c22e -->
# Исправление проблемы авторизации в 3x-ui панели и улучшение механизма карантина

## Проблема

Каждые 5 минут появляются ошибки "Could not log in to host" для хостов "🇪🇪 Эстония Test" и "🇫🇮 Финляндия 1 Test", хотя данные авторизации верны и вход вручную работает.

## Причинно-следственная связь

1. **Ошибка авторизации**: Библиотека `py3xui` получает ошибку "Invalid username or password or two-factor code" при попытке входа
2. **Карантин**: После 3 неудачных попыток хосты попадают в карантин на 15 минут (900 секунд)
3. **Цикл**: Пока хост в карантине, функция `login_to_host` сразу возвращает `None, None` без попыток подключения
4. **Корневой путь URL**: В 3x-ui настроен корневой путь панели (например `/configpanel/` или `/manager/`) как средство защиты. URL должен включать этот путь, но библиотека `py3xui` может неправильно его обрабатывать
5. **Карантин не очищается**: При успешном подключении карантин не очищается автоматически, что приводит к задержкам восстановления работы

## Анализ

Из логов видно:

- `https://serv2.dark-maximus.com:8443/configpanel/` - успешное подключение
- `https://serv1.dark-maximus.com:8443/configpanel` - ошибка авторизации (после исправления URL)
- `https://shop.karumweb.ru:8443/manager` - успешное подключение (после удаления trailing slash)

**Вывод**: Удаление trailing slash из URL решает проблему для некоторых хостов. Библиотека `py3xui` корректно обрабатывает URL без trailing slash.

## Решение (безопасное)

### 1. Нормализация формата URL (безопасная)

Добавить функцию нормализации URL, которая:

- Удаляет trailing slash из URL (как показано в успешных случаях)
- Удаляет двойные слеши
- Сохраняет протокол, домен, порт и путь
- Не изменяет URL, если он уже в правильном формате

**Безопасность**: Функция должна быть идемпотентной и не ломать существующие рабочие URL.

### 2. Очистка карантина при успешном подключении

Добавить очистку карантина в блок успешного подключения:

- Очищать карантин сразу после успешного `api.login()`
- Использовать безопасный метод удаления из словаря (`dict.pop()` с default)
- Логировать очистку карантина для диагностики

**Безопасность**: Использовать `dict.pop(host_url, None)` вместо `del` для избежания KeyError.

### 3. Улучшение обработки ошибок

Следовать паттернам из `src/shop_bot/utils/error_handler.py`:

- Конкретные исключения вместо общего `Exception`
- Логирование с контекстом
- Безопасная обработка исключений при очистке карантина

**Безопасность**: Очистка карантина должна быть в `try-except`, чтобы не сломать основной поток при ошибке.

### 4. Улучшение логирования

Добавить детальное логирование:

- Логирование нормализованного URL перед попыткой подключения
- Логирование успешной очистки карантина
- Логирование деталей ошибки авторизации (без паролей)

**Безопасность**: Не логировать пароли или чувствительные данные.

### 5. Добавление параметра use_tls_verify

Добавить параметр `use_tls_verify=False` при создании экземпляра `Api` для работы с самоподписанными сертификатами.

**Безопасность**: Параметр должен быть опциональным и не ломать существующие вызовы.

## Файлы для изменения

1. `src/shop_bot/modules/xui_api.py`:

- Добавить функцию `_normalize_host_url()` для безопасной нормализации URL
- Изменить функцию `login_to_host()`:
- Использовать нормализованный URL для создания `Api`
- Очищать карантин при успешном подключении (в блоке try-except)
- Улучшить обработку исключений
- Добавить параметр `use_tls_verify=False` при создании `Api`
- Улучшить логирование

## Безопасность изменений

1. **Обратная совместимость**: Все изменения должны быть обратно совместимыми
2. **Идемпотентность**: Нормализация URL не должна изменять уже правильные URL
3. **Обработка ошибок**: Все операции с карантином должны быть в try-except
4. **Логирование**: Не логировать чувствительные данные (пароли, токены)
5. **Тестирование**: Проверить на существующих рабочих хостах перед применением

## Тестирование

После исправления проверить:

1. Успешное подключение к хостам "🇪🇪 Эстония Test" и "🇫🇮 Финляндия 1 Test"
2. Отсутствие ошибок в логах
3. Корректная работа синхронизации ключей
4. Автоматическая очистка карантина при успешном подключении
5. Правильная обработка URL с корневыми путями (`/configpanel`, `/manager`)
6. Обратная совместимость с существующими рабочими хостами
7. Отсутствие регрессий в других функциях

### To-dos

- [ ] Добавить функцию нормализации URL для извлечения базового URL (протокол + домен + порт) без дополнительных путей
- [ ] Изменить функцию login_to_host для использования нормализованного URL при создании экземпляра Api
- [ ] Добавить очистку карантина при успешном подключении в функции login_to_host
- [ ] Улучшить логирование: добавить логирование нормализованного URL и очистки карантина