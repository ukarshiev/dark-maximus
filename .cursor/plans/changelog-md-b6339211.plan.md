<!-- b6339211-b289-4d1a-9ab7-1e4f75421ee4 b5e0f35a-a807-4978-8e14-2062c962f76b -->
# План обновления упоминаний старых портов в CHANGELOG.md

## Цель

Обновить все упоминания старых портов на новые в CHANGELOG.md для соответствия текущей конфигурации проекта.

## Замены

- `3001` → `50001` (docs сервис)
- `3002` → `50002` (codex-docs сервис)
- `3003` → `50003` (user-cabinet сервис)
- `5051` → `50004` (allure-service внутренний порт)

## Файлы для изменения

- `CHANGELOG.md` - обновить все упоминания старых портов в исторических записях

## Шаги выполнения

1. **Обновить строку 304** (актуальная запись):

- Заменить `localhost:3003` на `localhost:50003` в описании изменения для `deploy/nginx/dark-maximus.conf.tpl`

2. **Обновить исторические записи** (строки 2-13, 80-81, 2346, 2410-2411, 2595, 3009-3012, 3111, 313, 316):

- Заменить все упоминания `3001` на `50001`
- Заменить все упоминания `3002` на `50002`
- Заменить все упоминания `3003` на `50003`
- Заменить все упоминания `5051` на `50004` (кроме внутренних URL между контейнерами)

3. **Проверить контекст замен**:

- Убедиться, что замены не затрагивают даты и версии
- Убедиться, что замены не затрагивают внутренние URL между контейнерами (например, `allure-service:5050`)

4. **Проверить результат**:

- Выполнить grep для проверки отсутствия старых портов в CHANGELOG.md
- Убедиться, что все замены выполнены корректно

## Важные замечания

- Замены выполняются только в тексте описаний изменений
- Даты и версии не изменяются
- Внутренние URL между контейнерами (например, `allure-service:5050`) не изменяются

### To-dos

- [ ] Обновить строку 304 в CHANGELOG.md: заменить localhost:3003 на localhost:50003
- [ ] Обновить все исторические записи в CHANGELOG.md: заменить 3001→50001, 3002→50002, 3003→50003, 5051→50004
- [ ] Проверить результат замен через grep и убедиться, что все старые порты обновлены