---
alwaysApply: true
---
# 📋 Правило организации и актуализации документации Dark Maximus

> **Применение:** при любом изменении кода, добавлении или создании документа в проекте Nx

## 🎯 Основной принцип

**Следуй фреймворку [Diátaxis](https://diataxis.fr/) и существующей структуре документации.**  
Документация каждого приложения или библиотеки хранится **внутри соответствующей папки `docs/`**.  
Редактирование или создание `.md` файлов **в корне репозитория запрещено**.

## 🐳 Сервисы проекта

Правило применяется ко всем сервисам проекта:

1. **dark-maximus-bot** (Telegram бот) — документация в `docs/`
2. **dark-maximus-docs** (Пользовательская Wiki) — документация в `docs/guides/user/` и `docs/internal/wiki/`
3. **dark-maximus-user-cabinet** (Личный кабинет) — документация в `docs/`
4. **dark-maximus-monitoring** (Тесты и мониторинг) — документация в `docs/guides/testing/`
5. **dark-maximus-codex-docs** (Админская документация) — документация в `codex.docs/` и `docs/admin/`
6. **dark-maximus-allure** (Allure Service) — документация в `docs/guides/testing/allure-reporting.md`
7. **dark-maximus-allure-homepage** (Allure Homepage) — документация в `docs/guides/testing/allure-reporting.md`

**Важно:** При изменении любого сервиса обновляй соответствующую документацию согласно структуре Diátaxis.

## 📚 Ссылки на структуру

- **[Структура документации](docs/architecture/documentation-structure.md)** — описание организации папок  
- **[Сводка по документации](docs/internal/documentation-summary.md)** — краткий обзор  
- **[README документации](docs/README.md)** — главная страница

## 🔄 Алгоритм обновления и создания

1. **Определи тип контента** по Diátaxis:  
   - Tutorials → `docs/tutorials/`  
   - Guides → `docs/guides/{audience}/`  
   - Reference → `docs/reference/`  
   - Architecture → `docs/architecture/`
2. **При поиске информации для документации** следуй приоритету из `stack-docs-sourse.mdc`:  
   1) MCP источники (Ref, Context7), 2) официальная документация
3. **Проверяй содержимое `docs/`** при каждом изменении кода.  
   При необходимости обновляй диаграммы (PlantUML, Mermaid), примеры и ссылки.  
4. **Следуй правилам именования:** kebab-case, без пробелов, без кириллицы.  
5. **Добавляй обязательные элементы:** заголовок, дата последней редакции, описание.  
6. **Обновляй связанные файлы:** `README.md` родительской папки.  
7. **При работе через MCP (`nx mcp`)** обновляй документацию только в пределах изменённых модулей.  

## 🚫 Запрещено

- Создавать или хранить `.md` файлы в корне репозитория (`/`), **кроме `CHANGELOG.md`** (это стандартное исключение).  
- Создавать файлы в `/docs`, кроме `README.md`.  
- Смешивать типы контента в одной папке.  
- Использовать кириллицу в именах файлов.  

## 🧩 Автоматическая актуализация

Если документация устарела — актуализируй:  
- редактируй или добавляй нужные разделы;  
- обновляй диаграммы;  
- корректируй примеры и внутренние ссылки без поломки структуры.  

## 📦 Логи и инструкции

- Все инструкции и примеры команд (Nx, Docker, PowerShell) должны направлять вывод логов в каталог `logs/` внутри репозитория.
- Для PowerShell используем `Join-Path (Resolve-Path ./logs)` вместо временных директорий (`$env:TEMP`).
- При добавлении новых скриптов/гайдов проверяй, что пути к журналам указывают на `logs/`.

## ✏️ Стиль

Пиши просто и понятно, без лишней техничности.  
Дата последней редакции указывается **в начале каждого изменённого `.md` файла**.

## 📑 Формат справочников меню

- Для описания меню используем вертикальные таблицы с двумя колонками «Атрибут» и «Значение».  
- Обязательные атрибуты: Меню, Триггер, Текст, Кнопки, Условия / режимы, Переходы.  
- Раздел «📬 Шаблоны категории “Покупка”» оформляем карточками: Название, Код, Категория, Текст, Кнопки, Описание.  
- После изменения логики меню обновляем связанные диаграммы (Mermaid, PlantUML) в этом же документе.

## 🧾 Версии и журнал изменений

- Любые существенные правки документации фиксируем в `CHANGELOG.md` (новая верхняя запись с текущим временем UTC+3).  
- Синхронно обновляем версию проекта в `pyproject.toml`.  
- Проверяем и при необходимости обновляем `docs/documentation-inventory.md` и README родительских разделов.

## 🔍 Поиск и проверка

- После крупных изменений выполняем пересборку документации (`npx nx build docs` или `npx nx serve docs`), чтобы обновить поисковый индекс.  
- Прогоняем `read_lints` для всех изменённых `.md` файлов.  
- Следим, чтобы таблицы, списки и заголовки соответствовали принятому формату (без смешения типов элементов).

---

**Подробнее см.:** [docs/architecture/documentation-structure.md](docs/architecture/documentation-structure.md)
