---
alwaysApply: true
---
## Стили и UI

- Сохраняй существующую визуальную идентичность проекта: тёмная цветовая схема, акцентный зелёный цвет `#008771`, аккуратные контрасты и читаемость.
- Улучшай UX и визуальную чистоту, не ломая текущий дизайн.

### Фреймворки и архитектура
- Используй **Material-UI (MUI)** как основную дизайн-систему:
  - Библиотеки: `@mui/material`, `@mui/icons-material`.
  - Разметка: `Box`, `Grid`, `Stack`.
  - Темизация: `ThemeProvider` + `createTheme`; все отступы, цвета и размеры — из темы.
  - Стилизация через `sx` и MUI System. Избегай кастомных CSS, если аналог можно сделать средствами MUI.
- Используй **Tailwind CSS** как основной **утилитарный CSS-фреймворк**:
  - Применяй утилитарные классы для базовой компоновки и адаптива.
  - Не дублируй MUI-функционал — Tailwind дополняет, а не заменяет MUI.
- Архитектура стилей должна быть **модульной**, с использованием **CSS-переменных** (`:root { --color-bg: ... }` и т. д.) для управления темами и переиспользованием значений.

### Цветовая схема и тема
- Основная тема — **тёмная**, с зелёным акцентом `#008771`.
- Используй `theme.palette.mode = 'dark'` и акцент из `theme.palette.primary`.
- Tailwind-цвета синхронизируй с MUI через `theme.extend.colors`.

### Адаптивность
- Интерфейс должен быть полностью адаптивен для мобильных и планшетов.
- Используй `theme.breakpoints` и Tailwind-медиаутилиты (`sm:`, `md:`, `lg:`).
- Избегай “магических” пикселей — всё через системные отступы, breakpoints и theme.spacing.

### Компоненты и поведение
- Все новые компоненты базируй на MUI.
- Типографика — через `Typography` с `variant`.
- Иконки — через MUI icons с настройкой `fontSize` и `sx`.
- Контекстные меню (`kebab`) — не позиционируй вручную (`right: 0` / `left: 0`); используй `anchorEl` и `anchorOrigin`.
- Добавляй `aria-*` атрибуты, tooltips и hover-состояния для доступности.

## 🚫 КРИТИЧЕСКОЕ ПРАВИЛО: Защита кебаб-меню от поломки

**ЗАПРЕЩЕНО изменять CSS/JS кебаб-меню без явного запроса пользователя.**

### Что НИКОГДА нельзя делать:

1. **НЕ МЕНЯТЬ `position` у следующих элементов:**
   - `.kebab-wrapper` — ВСЕГДА `position: relative` (НЕ `static`, НЕ `absolute`, НЕ `fixed`)
   - `.actions-cell` — ВСЕГДА `position: relative` (НЕ `static`)
   - `.kebab-menu` — базовое состояние `position: absolute`, активное `.kebab-menu.active` — `position: fixed !important`

2. **НЕ МЕНЯТЬ z-index без крайней необходимости:**
   - `.kebab-menu` — `z-index: 999999 !important`
   - `.kebab-menu.active` — `z-index: 999999 !important` + `isolation: isolate`
   - `.kebab-wrapper` — НЕ должен иметь z-index (создаёт stacking context)
   - `.kebab-btn` — НЕ должен иметь z-index

3. **НЕ МЕНЯТЬ логику JavaScript функций:**
   - `toggleKebabMenu(menuId)` — закрывает другие меню, переключает текущее, вызывает `adjustKebabMenuPosition`
   - `adjustKebabMenuPosition(menu)` — использует двойной `requestAnimationFrame`, устанавливает `top` и `left` через `setProperty` с `important`
   - `closeKebabMenu(menuId)` — удаляет классы `active` и `open-up`, сбрасывает inline стили

4. **НЕ УДАЛЯТЬ критичные CSS свойства:**
   - `.kebab-menu.active` — ОБЯЗАТЕЛЬНО `position: fixed !important`, `z-index: 999999 !important`, `isolation: isolate`
   - `.actions-cell` — ОБЯЗАТЕЛЬНО `overflow: visible`, `position: relative`
   - `.table-scroll-x` — ОБЯЗАТЕЛЬНО `overflow: visible` (на десктопе)

### Что МОЖНО делать только с явного разрешения:

- Изменять визуальные стили (цвета, размеры, отступы) кнопки `.kebab-btn`
- Изменять стили элементов меню `.kebab-item` (кроме position и z-index)
- Добавлять новые элементы в меню (HTML)
- Изменять позиционирование только если пользователь ЯВНО просит исправить конкретную проблему

### Обязательные проверки перед любыми изменениями:

1. **Перед изменением CSS/JS кебаб-меню:**
   - Спросить пользователя: "Вы хотите изменить кебаб-меню?"
   - Если нет явного запроса — НЕ ТРОГАТЬ

2. **Если пользователь просит исправить проблему:**
   - Сначала проверить в браузере текущее состояние
   - Изменить ТОЛЬКО то, что нужно для исправления проблемы
   - Протестировать в браузере после изменений
   - НЕ делать "улучшений" без запроса

3. **При работе с другими элементами:**
   - Проверить, не влияют ли изменения на кебаб-меню
   - Если влияют — предупредить пользователя и получить разрешение

### Текущая рабочая реализация (НЕ МЕНЯТЬ без запроса):

**CSS структура:**
- `.kebab-wrapper { position: relative; display: inline-block; }`
- `.kebab-menu { position: absolute; z-index: 999999 !important; display: none; }`
- `.kebab-menu.active { position: fixed !important; z-index: 999999 !important; isolation: isolate; }`
- `.actions-cell { overflow: visible; position: relative; }`

**JavaScript функции:**
- `toggleKebabMenu()` — закрывает другие меню, переключает текущее
- `adjustKebabMenuPosition()` — позиционирует через `setProperty` с `important` в двойном `requestAnimationFrame`
- `closeKebabMenu()` — очищает классы и inline стили

### Если случайно сломал:

1. Немедленно откатить изменения
2. Восстановить рабочую версию из этого правила
3. Извиниться перед пользователем
4. Больше НЕ ТРОГАТЬ без явного запроса