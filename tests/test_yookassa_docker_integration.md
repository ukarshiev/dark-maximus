# Руководство по тестированию YooKassa и Docker Management

> **Дата создания:** 08.11.2025  
> **Версия:** 1.0  
> **Цель:** Проверка функциональности YooKassa и Docker Management после реализации

## Предварительные требования

1. Запущенное приложение Dark Maximus
2. Доступ к админ-панели (логин/пароль)
3. Доступ к логам приложения
4. Docker и docker-compose установлены
5. PowerShell 7+ (для Windows)

## Часть 1: Тестирование YooKassa

### 1.1. Проверка переключения режима test/production

**Цель:** Убедиться, что индикаторы режима отображаются корректно и синхронизируются после рестарта.

**Шаги:**

1. Откройте браузер и перейдите в админ-панель:
   ```
   http://localhost:3001/login
   ```

2. Войдите с учетными данными:
   - Логин: `panel-maximus`
   - Пароль: `F^h6xW!z&TGsN@dC%UeI4hXfeQFt0vw%*GA%t^J0#6Ba#@lN^&`

3. Перейдите в **Настройки → Платежные системы**

4. Найдите раздел "YooKassa" и проверьте текущие индикаторы:
   - **"В БД:"** — показывает режим, сохраненный в базе данных
   - **"Активный режим бота:"** — показывает реально используемый режим

5. Проверьте соответствие:
   - Если индикаторы не совпадают → должно быть предупреждение "Требуется перезапуск бота"
   - Если индикаторы совпадают → предупреждения нет

6. Переключите чекбокс "Тестовый режим YooKassa"

7. Нажмите **"Сохранить все настройки"**

8. Обновите страницу (F5)

9. Проверьте:
   - Индикатор "В БД" изменился
   - Индикатор "Активный режим бота" остался прежним
   - Появилось предупреждение о необходимости рестарта

**Ожидаемый результат:**
- ✅ Индикаторы отображаются корректно
- ✅ Предупреждение появляется при несоответствии
- ✅ Цветовая индикация: 🟡 Тестовый, 🟢 Боевой, 🔴 Неизвестно

---

### 1.2. Проверка логирования при создании платежа

**Цель:** Убедиться, что режим YooKassa логируется при каждом создании платежа.

**Шаги:**

1. Откройте PowerShell и перейдите в директорию проекта:
   ```powershell
   cd E:\GitHub\dark-maximus
   ```

2. Запустите мониторинг логов:
   ```powershell
   npx --yes nx logs bot --tail=200 2>&1 | Select-String -Pattern 'YOOKASSA_PAYMENT|YOOKASSA_RECONFIGURE' -Context 2,0 -Wait -NoEmphasis; Get-Date; Start-Sleep -Seconds 2
   ```

3. В другом окне/вкладке откройте Telegram и найдите вашего бота

4. Создайте тестовый платёж:
   - Отправьте `/start`
   - Выберите "Пополнить баланс" или "Купить ключ"
   - Укажите сумму (например, 100 рублей)
   - Выберите "YooKassa" как способ оплаты

5. Вернитесь к PowerShell и проверьте логи

**Ожидаемый вывод:**

```
[YOOKASSA_RECONFIGURE] mode=ТЕСТОВЫЙ, shop_id=12345678..., api_url=https://api.test.yookassa.ru/v3, verify_ssl=True
[YOOKASSA_PAYMENT_TOPUP] user_id=123456789, amount=100.0 RUB, test_mode=True, active_shop_id=12345678...
```

**Проверьте:**
- ✅ `[YOOKASSA_RECONFIGURE]` присутствует в логах
- ✅ `mode=ТЕСТОВЫЙ` или `mode=БОЕВОЙ` отображается корректно
- ✅ `shop_id` логируется (первые 8 символов)
- ✅ `api_url` соответствует режиму
- ✅ `[YOOKASSA_PAYMENT_TOPUP]` присутствует
- ✅ `test_mode` соответствует текущему режиму

---

### 1.3. Рестарт бота и проверка синхронизации режима

**Цель:** Убедиться, что после рестарта индикатор "Активный режим бота" синхронизируется с БД.

**Шаги:**

1. В админ-панели нажмите кнопку **"Рестарт бота"** в header (правый верхний угол)

2. В модальном окне проверьте:
   - Заголовок: "Перезапуск бота"
   - Сообщение: "Это перезапустит только бот. Продолжить?"
   - Кнопки: "Отмена" и "Подтвердить"

3. Нажмите **"Подтвердить"**

4. Наблюдайте за модальным окном прогресса:
   - Появился спиннер (анимация вращения)
   - Сообщение "Выполняется..."
   - Таймер обратного отсчёта "Страница обновится через N сек..."

5. Дождитесь автоматического обновления страницы (~20 секунд)

6. Снова перейдите в **Настройки → Платежные системы**

7. Проверьте индикаторы:
   - "В БД" и "Активный режим бота" теперь совпадают
   - Предупреждение о несоответствии исчезло

**Ожидаемый результат:**
- ✅ Модальное окно подтверждения работает
- ✅ Модальное окно прогресса отображается
- ✅ Таймер работает корректно
- ✅ Страница автоматически обновляется
- ✅ Индикаторы синхронизированы

---

### 1.4. Проверка webhook с полем test

**Цель:** Убедиться, что webhook от YooKassa логирует поле `test` и проверяет соответствие режимов.

**Шаги:**

1. Запустите мониторинг webhook логов:
   ```powershell
   npx --yes nx logs bot --tail=500 2>&1 | Select-String -Pattern 'YOOKASSA_WEBHOOK' -Context 2,0 -Wait -NoEmphasis; Get-Date; Start-Sleep -Seconds 2
   ```

2. Создайте тестовый платёж и оплатите его (используйте тестовую карту YooKassa)

3. Дождитесь webhook от YooKassa

**Ожидаемый вывод:**

```
[YOOKASSA_WEBHOOK] event=payment.succeeded, payment_id=2c67e8f9-000f-5000-8000-18db351245c7, test=True, paid=True
```

**Если режимы не совпадают:**

```
[YOOKASSA_WEBHOOK] ⚠️ MODE MISMATCH! webhook test=True, db test_mode=False, payment_id=..., Проверьте настройки YooKassa в админ-панели!
```

**Проверьте:**
- ✅ Поле `test` логируется
- ✅ Поля `event`, `payment_id`, `paid` логируются
- ✅ При несоответствии режимов выводится предупреждение `MODE MISMATCH`
- ✅ Защита от дублирования: повторный webhook с тем же payment_id пропускается

---

## Часть 2: Тестирование Docker Management

### 2.1. Кнопка "Рестарт бота"

**Цель:** Проверить работу кнопки перезапуска бота.

**Шаги:**

1. В админ-панели найдите в header кнопку **"Рестарт бота"** (иконка робота)

2. Нажмите на кнопку

3. Проверьте модальное окно подтверждения:
   - **Заголовок:** "Перезапуск бота"
   - **Сообщение:** "Это перезапустит только бот. Продолжить?"
   - **Кнопки:** "Отмена" (серая) и "Подтвердить" (зеленая)

4. Нажмите **"Отмена"** → модальное окно должно закрыться

5. Снова нажмите **"Рестарт бота"**

6. Нажмите **"Подтвердить"**

7. Проверьте модальное окно прогресса:
   - Спиннер (вращающаяся анимация)
   - Заголовок: "Перезапуск бота"
   - Сообщение: "Выполняется..."
   - Таймер: "Страница обновится через 20 сек..."

8. Дождитесь автоматического обновления страницы

9. Проверьте, что бот работает:
   - Статус на главной панели: "Запущен"
   - Бот отвечает в Telegram

**Ожидаемый результат:**
- ✅ Кнопка видна и кликабельна
- ✅ Модальное окно подтверждения работает
- ✅ Кнопка "Отмена" закрывает модальное окно
- ✅ Модальное окно прогресса отображается
- ✅ Таймер отсчитывает секунды
- ✅ Страница обновляется через ~20 секунд
- ✅ Бот перезапустился и работает

---

### 2.2. Кнопка "Рестарт всего"

**Цель:** Проверить работу кнопки перезапуска всех сервисов.

**Шаги:**

1. Нажмите кнопку **"Рестарт всего"** в header (иконка синхронизации)

2. Проверьте модальное окно:
   - **Заголовок:** "Перезапуск всех сервисов"
   - **Сообщение:** "Это перезапустит все сервисы (bot, docs, codex-docs). Продолжить?"

3. Нажмите **"Подтвердить"**

4. Наблюдайте за прогрессом (таймер ~35 секунд)

5. После автообновления проверьте статус всех сервисов:
   ```powershell
   docker compose ps; Get-Date; Start-Sleep -Seconds 1
   ```

6. Все сервисы должны быть в состоянии "Up"

**Ожидаемый результат:**
- ✅ Кнопка работает
- ✅ Модальное окно корректно
- ✅ Таймер ~35 секунд
- ✅ Все сервисы перезапустились

---

### 2.3. Кнопка "Ребилд" (опционально)

**⚠️ ВНИМАНИЕ:** Эта операция занимает 3-5 минут. Рекомендуется тестировать только при необходимости.

**Цель:** Проверить работу полного ребилда.

**Шаги:**

1. Нажмите кнопку **"Ребилд"** (красная кнопка с молотком)

2. Проверьте модальное окно:
   - **Заголовок:** "Ребилд без кеша"
   - **Сообщение:** "Это полностью пересоберёт образ без кеша (займёт 3-5 минут). Продолжить?"

3. Нажмите **"Подтвердить"**

4. Наблюдайте за таймером (~200 секунд)

5. Дождитесь завершения

**Ожидаемый результат:**
- ✅ Кнопка работает
- ✅ Предупреждение о длительности
- ✅ Сборка завершилась успешно
- ✅ Все сервисы запустились

---

### 2.4. Проверка логирования Docker операций

**Цель:** Убедиться, что все Docker операции логируются.

**Шаги:**

1. Откройте файл логов:
   ```powershell
   Get-Content logs/application.log -Tail 50 | Select-String -Pattern 'DOCKER_API'; Get-Date; Start-Sleep -Seconds 1
   ```

2. Выполните любую Docker операцию (например, "Рестарт бота")

3. Снова проверьте логи

**Ожидаемый вывод:**

```
[DOCKER_API] restart-bot initiated by panel-maximus
[DOCKER_API] restart-bot completed: returncode=0
```

**Проверьте:**
- ✅ Каждая операция логируется дважды: initiated и completed
- ✅ Username администратора присутствует
- ✅ Return code указан (0 = успешно)

---

## Часть 3: Проверка обработки ошибок

### 3.1. Docker не запущен

**Шаги:**

1. Остановите Docker Desktop (Windows) или Docker service (Linux)

2. В админ-панели нажмите "Рестарт бота"

3. Подтвердите действие

**Ожидаемый результат:**
- ✅ Модальное окно показывает ошибку
- ✅ Сообщение об ошибке понятное
- ✅ Таймер не запускается
- ✅ Ошибка залогирована

### 3.2. Timeout операции

**Примечание:** Этот тест сложно воспроизвести без имитации зависания.

**Ожидаемое поведение:**
- Timeout сработает через 90 секунд (restart) или 300 секунд (rebuild)
- Пользователь увидит сообщение об ошибке
- Операция прервётся

---

## Часть 4: Финальная проверка

### Чек-лист успешного тестирования

**YooKassa:**
- [ ] Индикаторы режима отображаются корректно
- [ ] Предупреждение о несоответствии работает
- [ ] Логирование при создании платежа присутствует
- [ ] Webhook логирует поле `test`
- [ ] Защита от дублирования webhook работает
- [ ] Рестарт синхронизирует режимы

**Docker Management:**
- [ ] Кнопка "Рестарт бота" работает
- [ ] Кнопка "Рестарт всего" работает
- [ ] Кнопка "Ребилд" работает (опционально)
- [ ] Модальные окна отображаются корректно
- [ ] Таймеры работают
- [ ] Автообновление страницы работает
- [ ] Логирование операций присутствует
- [ ] Обработка ошибок работает

**Документация:**
- [ ] Раздел "Docker Management" добавлен в admin-panel-guide.md
- [ ] Документ docker-socket-risks.md создан
- [ ] CHANGELOG.md обновлён

---

## Отчёт о тестировании

**Дата:** _______________  
**Тестировщик:** _______________  
**Версия:** 3.22.1

| Функция | Статус | Комментарий |
|---------|--------|-------------|
| YooKassa: Индикаторы режима | ⬜ Пройдено / ⬜ Провалено | |
| YooKassa: Логирование платежей | ⬜ Пройдено / ⬜ Провалено | |
| YooKassa: Webhook с test | ⬜ Пройдено / ⬜ Провалено | |
| Docker: Рестарт бота | ⬜ Пройдено / ⬜ Провалено | |
| Docker: Рестарт всего | ⬜ Пройдено / ⬜ Провалено | |
| Docker: Ребилд | ⬜ Пройдено / ⬜ Провалено / ⬜ Пропущено | |
| Docker: Логирование | ⬜ Пройдено / ⬜ Провалено | |
| Документация | ⬜ Пройдено / ⬜ Провалено | |

**Общий результат:** ⬜ Все тесты пройдены / ⬜ Есть проблемы

**Найденные проблемы:**
1. 
2. 
3. 

**Рекомендации:**
1. 
2. 
3. 

---

**Примечание:** Этот документ можно использовать как чек-лист при ручном тестировании или как основу для автоматизированных тестов.

