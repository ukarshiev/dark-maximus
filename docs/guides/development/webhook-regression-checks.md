# 🔁 Регрессия вебхуков: быстрый чек-лист

> Дата последней редакции: 10.11.2025

Короткая инструкция для разработчиков по обязательной проверке после любых изменений в обработчиках платежных вебхуков.

## Шаги проверки

1. `python -m compileall src/shop_bot/webhook_server/app.py`  
   - Гарантирует, что файл компилируется и не содержит синтаксических ошибок ещё до сборки образа.
2. `npx --yes nx serve bot` (короткий прогон)  
   - Проверяем, что контейнер поднимается, зависимости и точки входа валидны.
3. `npx --yes nx stop bot`  
   - Останавливаем сервис после проверки, чтобы освободить ресурсы.

## Что это даёт

- **Мгновенный контроль синтаксиса.** `compileall` обрывает цепочку ещё до запуска Docker, если в коде ошибка (как было с двойным `else`).
- **Проверка рантайма.** Быстрый `nx serve bot` убеждает, что бот стартует и модули импортируются. Это ловит проблемы с зависимостями и путями.
- **Экономия времени.** Вместо долгой Docker-сборки и раскопок логов — моментальная обратная связь локально.

## Интеграция в процесс

- Добавьте шаги 1–3 в собственный чек-лист перед коммитом правок вебхуков.
- Фиксируйте результат в описании MR/PR, чтобы ревьюеры видели успешные прогоны.
- При автоматизации CI можно завернуть команды в отдельный таргет Nx и использовать его в каждом пайплайне.

