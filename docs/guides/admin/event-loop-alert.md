# 🚨 Алерт по событию «Event loop is not available»

> Дата последней редакции: 10.11.2025

Инструкция для администраторов, как контролировать повторяющиеся сбои event loop при обработке платежей бота.

## Где отображается

- **Grafana Alerts** — уведомление появляется в разделе Alerting → Alert rules.
- **Уведомления** — переотправляем alert в Telegram-канал операций (бот `@dark_maximus_ops_bot`). Настраивается через Grafana Contact points.

## Настройка правила

1. **Источник логов**: поток `dark-maximus-bot` (Loki или другой используемый агрегатор логов).
2. **Запрос**:
   ```promql
   sum by (container) (rate({container="dark-maximus-bot"} |= "Event loop is not available" [5m]))
   ```
3. **Условие**: `> 3` за 5 минут — значит бот трижды подряд не смог получить event loop.
4. **Гистерезис**: 2 минуты (alert автоматически снимается, когда значение опускается ниже порога).
5. **Contact point**: Telegram (операционный канал) + Email on-call.

## Действия при оповещении

1. Проверить логи `dark-maximus-bot` — убедиться, что event loop реально не поднимается.
2. Перезапустить бот (`npx nx stop bot` → `npx nx serve bot`) и проверить, исчезла ли ошибка.
3. Если повторяется — открыть инцидент и приложить логи/в timestamps.

## Дополнительно

- При внедрении других лог-агрегаторов (ELK, Vector) используйте аналогичный порог — 3 срабатывания за 5 минут.
- В changelog инцидентов фиксируйте время срабатывания и принятые меры.

