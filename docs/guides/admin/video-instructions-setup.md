# Настройка и использование видеоинструкций

> Дата последней редакции: 15.01.2025  
> Тип документа: How-to guide

Практическое руководство по настройке и использованию функционала видеоинструкций в Dark Maximus.

## Быстрое применение изменений

После внесения изменений в код видеоинструкций:

1. **Перезапустите бота:**
   ```bash
   docker compose restart bot
   ```
   Перезапуск подтянет изменения кода из volume.

2. **Проверьте логи:**
   ```bash
   docker compose logs -f bot
   ```
   Убедитесь, что нет ошибок при запуске бота.

## Использование функционала

### Для администратора

#### Управление через веб-панель

Все видеоинструкции управляются через веб-панель администратора:

1. Перейдите в раздел **Инструкции** → вкладка **Видео**
2. Нажмите **Создать видео** для загрузки нового
3. Дважды кликните по записи в таблице для редактирования

#### Управление через Telegram-бота (устаревший способ)

> **Примечание:** Рекомендуется использовать веб-панель для управления видеоинструкциями.

1. **Загрузка видеоинструкций:**
   - Отправьте видео в бот с командой `/upload_video <платформа>`
   - Пример: `/upload_video android` (отправьте видео вместе с командой)
   - Доступные платформы: `android`, `ios`, `windows`, `macos`, `linux`

2. **Просмотр загруженных видео:**
   - Используйте команду `/list_videos`
   - Покажет все загруженные видеоинструкции с размерами

3. **Удаление видео:**
   - Используйте команду `/delete_video <платформа>`
   - Пример: `/delete_video android`

### Для пользователей

1. Перейдите в раздел **"🌐 Инструкции❓"** в боте
2. Выберите вашу операционную систему
3. Получите видеоинструкцию вместе с текстовой инструкцией

## Структура файлов

```
video_instructions/
├── videos/          # Видеофайлы (.mp4, .webm)
└── posters/         # Превью изображения (.jpg, .png, .webp)
```

### Формат файлов

**Видео:**
- Формат: MP4 (H.264/H.265), WebM
- Разрешение: рекомендуется 720p или 1080p
- Битрейт: 2-5 Mbps для оптимального соотношения качества и размера
- Имя файла: генерируется автоматически (например, `video_12345678.mp4`)

**Превью:**
- Формат: JPG, PNG, WebP
- Разрешение: рекомендуется 1280x720 (16:9)
- Размер: до 500 KB
- Имя файла: генерируется автоматически (например, `poster_12345678.jpg`)

## Настройки

В файле `src/shop_bot/config.py`:

```python
# Поддержка видеоинструкций
VIDEO_INSTRUCTIONS_ENABLED = True
VIDEO_INSTRUCTIONS_DIR = "video_instructions"
```

## Требования к видео

- **Формат**: MP4 (H.264/H.265), WebM
- **Максимальный размер**: 50MB (ограничение Telegram для прямых загрузок)
- **Рекомендуемое разрешение**: 720p или 1080p
- **Продолжительность**: до 5 минут
- **Битрейт**: 2-5 Mbps

## Размещение видео

После загрузки видео становится доступным по следующим URL:

- **Плеер (fullscreen)**: `/video/player/{video_id}`
- **Встраиваемый плеер**: `/video/embed/{video_id}`
- **Прямая ссылка**: `/video_instructions/videos/{filename}`

## Интеграция с Telegram Bot

Видео автоматически интегрируются с ботом через WebView:

- Пользователь нажимает кнопку "Видеоинструкция"
- Открывается WebView с полноэкранным видеоплеером
- Видео загружается с сервера без ограничений Telegram

## Очистка неиспользуемых файлов

Если видеоинструкция удаляется через панель, соответствующие файлы также удаляются автоматически.

## Связанные документы

- [Tutorial: Видеоинструкции](../../tutorials/video-instructions.md) — Подробное описание функционала и рекомендации по созданию видео
- [Руководство по админ-панели](admin-panel-guide.md) — Полное руководство по веб-интерфейсу администратора

