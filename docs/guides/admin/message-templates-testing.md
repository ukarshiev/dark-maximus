# 🧪 Тестирование шаблонов сообщений бота

> **Версия:** 1.0.0  
> **Обновлено:** 16.11.2025  
> **URL:** `http://localhost:50001` (разработка) или `https://your-domain.com` (продакшн)

## 📋 Содержание

1. [Описание](#описание)
2. [Назначение теста](#назначение-теста)
3. [Структура теста](#структура-теста)
4. [Запуск теста](#запуск-теста)
5. [Проверяемые сценарии](#проверяемые-сценарии)
6. [Allure отчёты](#allure-отчёты)
7. [Примеры использования](#примеры-использования)

---

## Описание

Unit-тесты для модуля форматирования сообщений бота проверяют корректность работы функций формирования сообщений в зависимости от режима предоставления тарифа и шаблонов из справочника "Тексты бота".

**Расположение:** `tests/unit/test_utils/test_message_templates.py`

**Зависимости:**
- `pytest` - фреймворк для тестирования
- `allure-pytest` - интеграция с Allure для отчётов
- `unittest.mock` - моки для внешних зависимостей

---

## Назначение теста

Тесты проверяют:

1. **Форматирование информации о тарифе** (`format_tariff_info`)
   - Корректное определение статусной иконки (✅/❌)
   - Извлечение флага страны из названия хоста
   - Форматирование названия тарифа и цены
   - Формирование итоговой строки с информацией о тарифе

2. **Генерация сообщений о покупке** (`get_purchase_success_text`)
   - Корректность генерации сообщений для всех режимов предоставления:
     - `key` - только ключ (VLESS connection string)
     - `subscription` - только подписка (subscription link)
     - `both` - ключ + подписка
     - `cabinet` - личный кабинет
     - `cabinet_subscription` - личный кабинет + подписка
   - Интеграция со справочником "Тексты бота" из БД
   - Fallback на код при отсутствии шаблона в БД

3. **Генерация информации о ключе** (`get_key_info_text`)
   - Корректность генерации информации для различных режимов
   - Наличие всех обязательных элементов (даты, статус, ключ/подписка)
   - Обработка пробных ключей

4. **Интеграция со справочником "Тексты бота"**
   - Получение шаблона из БД
   - Подстановка переменных в шаблон
   - Валидация HTML-тегов
   - Fallback при отсутствии или неактивности шаблона

---

## Структура теста

Тесты организованы в классы:

### 1. `TestFormatTariffInfo`
Тесты для функции `format_tariff_info()`:

- `test_format_tariff_info_normal_key` - обычный активный ключ
- `test_format_tariff_info_trial_key` - пробный ключ
- `test_format_tariff_info_expired_key` - истёкший ключ

### 2. `TestGetPurchaseSuccessText`
Тесты для функции `get_purchase_success_text()`:

- `test_get_purchase_success_text_provision_modes` - параметризованный тест для всех режимов предоставления
- `test_get_purchase_success_text_with_database_template` - интеграция со справочником "Тексты бота"

### 3. `TestGetKeyInfoText`
Тесты для функции `get_key_info_text()`:

- `test_get_key_info_text_provision_modes` - параметризованный тест для различных режимов
- `test_get_key_info_text_trial_key` - пробный ключ

### 4. `TestMessageTemplatesIntegration`
Тесты интеграции со справочником:

- `test_fallback_when_template_not_found` - fallback при отсутствии шаблона
- `test_fallback_when_template_inactive` - fallback при неактивном шаблоне

---

## Запуск теста

### Локальный запуск

```bash
# Запустить все тесты шаблонов сообщений
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py -v

# Запустить только unit-тесты шаблонов
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py -m unit -v

# Запустить с выводом подробной информации
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py -v -s

# Запустить конкретный класс тестов
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py::TestFormatTariffInfo -v

# Запустить конкретный тест
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py::TestFormatTariffInfo::test_format_tariff_info_normal_key -v
```

### Запуск через Nx

```bash
# Запустить все тесты
npx nx test bot

# Запустить только unit-тесты
npx nx test bot --testPathPattern="test_message_templates"
```

### Запуск с генерацией Allure отчёта

```bash
# Запустить тесты с генерацией Allure результатов
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py --alluredir=allure-results

# Просмотреть отчёт (требуется установка Allure CLI)
allure serve allure-results
```

**Доступ к веб-интерфейсу Allure:**
- Локальный доступ: `http://localhost:50005/allure-docker-service/projects/default/reports/latest/index.html`
- Внешний доступ (после SSL): `https://tests.dark-maximus.com/allure-docker-service/projects/default/reports/latest/index.html`

---

## Проверяемые сценарии

### 1. Форматирование информации о тарифе

**Обычный ключ:**
- ✅ Статусная иконка: "✅"
- ✅ Флаг хоста: первые 2 символа названия хоста
- ✅ Название тарифа: из параметра `plan_name`
- ✅ Форматирование цены: "X₽"

**Пробный ключ:**
- ✅ Статусная иконка: "✅"
- ✅ Название тарифа: "TRIAL"
- ✅ Форматирование цены: "0₽"

**Истёкший ключ:**
- ✅ Статусная иконка: "❌"
- ✅ Корректное форматирование цены с десятичными знаками

### 2. Генерация сообщений о покупке

**Режим `key`:**
- ✅ Наличие текста "НИЖЕ ВАШ КЛЮЧ"
- ✅ Наличие connection_string в сообщении
- ✅ Корректное форматирование даты истечения

**Режим `subscription`:**
- ✅ Наличие текста "ВАША ПОДПИСКА"
- ✅ Наличие subscription_link в сообщении

**Режим `both`:**
- ✅ Наличие текста "НИЖЕ ВАШ КЛЮЧ"
- ✅ Наличие текста "ВАША ПОДПИСКА"
- ✅ Наличие connection_string и subscription_link

**Режим `cabinet`:**
- ✅ Наличие текста "ВАШ ЛИЧНЫЙ КАБИНЕТ"
- ✅ Наличие cabinet_url в сообщении
- ✅ Генерация токена для личного кабинета

**Режим `cabinet_subscription`:**
- ✅ Наличие текста "ВАШ ЛИЧНЫЙ КАБИНЕТ"
- ✅ Наличие cabinet_url в сообщении
- ✅ Наличие subscription_link

### 3. Интеграция со справочником "Тексты бота"

**Получение шаблона из БД:**
- ✅ Функция `get_message_template` вызывается с правильным `template_key`
- ✅ Шаблон из БД используется при наличии активного шаблона
- ✅ Переменные корректно подставляются в шаблон
- ✅ HTML-валидация проходит успешно

**Fallback на код:**
- ✅ При отсутствии шаблона в БД используется fallback текст из кода
- ✅ При неактивном шаблоне (`is_active=0`) используется fallback текст
- ✅ Сообщение генерируется корректно даже без шаблона

---

## Allure отчёты

Тесты используют Allure для детального описания:

### Элементы Allure

1. **`@allure.title`** - краткое описание теста
2. **`@allure.description`** - подробное описание с HTML-разметкой
3. **`@allure.severity`** - уровень критичности (CRITICAL, NORMAL, MINOR)
4. **`@allure.tag`** - теги для фильтрации
5. **`@allure.step`** - шаги выполнения теста
6. **`allure.attach`** - прикрепление данных (входные параметры, результаты, сообщения)

### Пример отчёта

В Allure отчёте для каждого теста отображается:

- **Название теста** с параметрами (для параметризованных тестов)
- **Подробное описание** с проверяемыми аспектами
- **Шаги выполнения** с прикреплёнными данными:
  - Входные параметры (JSON)
  - Сгенерированные сообщения (HTML)
  - Результаты проверок (TEXT)
  - Длина сообщений (TEXT)

### Просмотр отчётов

1. **Запустить тесты с генерацией Allure результатов:**
   ```bash
   docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py --alluredir=allure-results
   ```

2. **Открыть веб-интерфейс Allure:**
   - Локальный доступ: `http://localhost:50005/allure-docker-service/projects/default/reports/latest/index.html`
   - Внешний доступ: `https://tests.dark-maximus.com/allure-docker-service/projects/default/reports/latest/index.html`

3. **Найти тесты:**
   - По тегу: `@allure.tag("purchase", "message")`
   - По severity: `@allure.severity(allure.severity_level.CRITICAL)`
   - По названию: фильтр по `@allure.title`

---

## Примеры использования

### Пример 1: Запуск теста для конкретного режима предоставления

```bash
# Запустить тест только для режима 'key'
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py::TestGetPurchaseSuccessText::test_get_purchase_success_text_provision_modes[key-key] -v
```

### Пример 2: Запуск теста с выводом всех шагов

```bash
# Запустить тест с подробным выводом шагов
docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py::TestGetPurchaseSuccessText::test_get_purchase_success_text_with_database_template -v -s --tb=short
```

### Пример 3: Просмотр результатов в Allure

1. Запустить тесты:
   ```bash
   docker compose exec autotest pytest tests/unit/test_utils/test_message_templates.py --alluredir=allure-results
   ```

2. Открыть веб-интерфейс:
   - Перейти на `http://localhost:50005/allure-docker-service/projects/default/reports/latest/index.html`
   - Найти тест `test_get_purchase_success_text_provision_modes`
   - Посмотреть шаги выполнения и прикреплённые данные

### Пример 4: Добавление нового теста

```python
@allure.title("Новый тест: проверка XYZ")
@allure.description("""
Подробное описание нового теста.
""")
@allure.severity(allure.severity_level.NORMAL)
@allure.tag("custom", "unit")
def test_custom_scenario(self, temp_db):
    """Новый тест"""
    with allure.step("Подготовка данных"):
        # Подготовка тестовых данных
        pass
    
    with allure.step("Вызов функции"):
        # Вызов тестируемой функции
        result = some_function()
        allure.attach(str(result), "Результат", allure.attachment_type.JSON)
    
    with allure.step("Проверка результата"):
        # Проверки
        assert result == expected_value
```

---

## Используемые фикстуры и моки

### Фикстуры

- **`temp_db`** - временная SQLite БД с полной структурой таблиц, включая `message_templates`
- **`mock_bot`** - мок для `aiogram.Bot` (если требуется)
- **`mock_xui_api`** - мок для `py3xui.Api` (если требуется)

### Моки

- **`get_message_template`** - мок для получения шаблона из БД
- **`get_or_create_permanent_token`** - мок для генерации токена личного кабинета
- **`get_user_cabinet_domain`** - мок для получения домена личного кабинета

---

## Рекомендации

1. **При добавлении новых режимов предоставления:**
   - Добавить новый режим в параметризацию `@pytest.mark.parametrize`
   - Обновить ожидаемые ключевые слова в `expected_keywords`
   - Добавить описание режима в `@allure.description`

2. **При изменении структуры шаблонов:**
   - Обновить тесты в `TestMessageTemplatesIntegration`
   - Проверить валидацию HTML-тегов
   - Обновить документацию

3. **При отладке тестов:**
   - Использовать `-s` флаг для вывода print-сообщений
   - Использовать `--tb=short` для краткого вывода ошибок
   - Проверять Allure отчёты для детальной информации

---

**Последнее обновление:** 16.11.2025

