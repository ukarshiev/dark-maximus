# 🛠️ Обслуживание пользовательской Wiki (Docsify)

> Дата последней редакции: 13.11.2025 13:06  
> Версия: 1.0

Этот файл заменяет `docs/user-docs/WIKI-README.md` и описывает, как работать с Docsify-версией пользовательской базы знаний (порт `3001`). Вся исходная статика перенесена в `docs/internal/wiki/`; экспорт для контейнера хранится в `docs/internal/wiki/public/`.

## 📂 Структура

```
docs/internal/wiki/
├── docsify-overview.md      # этот файл
├── sidebar-template.md      # пример `_sidebar.md`
└── user-portal-overview.md  # карта новой структуры
```

При публикации статики скопируйте нужные Markdown-файлы из основных разделов (`guides/user`, `tutorials/admin`, `reference/admin-panel`) и сформируйте `index.html` + `_sidebar.md` на стороне Docsify.

## 🚀 Запуск локально

```bash
docker compose up -d docs
```

После сборки откройте `http://localhost:3001`. Контейнер использует `Dockerfile.docs` и nginx в режиме unprivileged.

## 🔁 Обновление контента

1. Обновите Markdown-файлы в соответствующих разделах `docs/`.
2. Сформируйте `_sidebar.md` на основе `sidebar-template.md`.
3. Скопируйте необходимые файлы в `docs/internal/wiki/public/` (статичный экспорт).
4. Пересоберите контейнер: `docker compose build docs` → `docker compose up -d docs`.

## 🧪 Проверка

- Используйте встроенный поиск Docsify.
- Проверяйте, что ссылки не ведут на `docs/user-docs/*` — после миграции каталог удален.
- Для тестов на Windows сохраняйте логи в `logs/` (см. `docs/setup-scripts/README.md`).

## 📎 Полезные ссылки

- [Docsify documentation](https://docsify.js.org/)
- `docs/.nav.yml` — общая навигация MkDocs (ориентир при генерации меню).
- `docs/documentation-inventory.md` — инвентаризация разделов.


