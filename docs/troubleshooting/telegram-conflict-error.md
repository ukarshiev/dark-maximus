# Диагностика и решение TelegramConflictError

> Последнее обновление: 25.11.2025

## Описание проблемы

Ошибка `TelegramConflictError: Conflict: terminated by other getUpdates request` возникает, когда несколько экземпляров бота пытаются использовать метод `getUpdates` (polling) одновременно с одним и тем же токеном.

### Симптомы

- В логах появляется ошибка:
  ```
  TelegramConflictError: Telegram server says - Conflict: terminated by other getUpdates request; 
  make sure that only one bot instance is running
  ```
- Бот не получает обновления от Telegram
- Сообщения пользователей не обрабатываются

## Причины возникновения

Telegram Bot API не позволяет нескольким экземплярам бота использовать `getUpdates` одновременно с одним токеном. Конфликт может возникнуть в следующих случаях:

1. **Бот запущен на другом сервере/машине** с тем же токеном
2. **Бот был запущен ранее** и не был корректно остановлен (старый процесс все еще работает)
3. **Бот запущен локально** (вне Docker) одновременно с Docker контейнером
4. **Контейнер был перезапущен**, но старый процесс polling все еще работает
5. **Установлен webhook** для бота, но код пытается использовать polling

## Автоматическая диагностика и решение

Начиная с версии, включающей улучшения обработки конфликтов, система автоматически:

1. **Проверяет наличие webhook** перед запуском polling
2. **Удаляет webhook**, если он установлен (polling и webhook не могут работать одновременно)
3. **Логирует подробную информацию** о конфликте при его обнаружении

### Логи при автоматической проверке webhook

```
⚠️ ShopBot: Обнаружен установленный webhook: https://example.com/webhook. 
Удаляем webhook для использования polling...
✅ ShopBot: Webhook успешно удален. Можно запускать polling.
```

### Логи при обнаружении конфликта

```
❌ ShopBot: TelegramConflictError - Конфликт при получении обновлений!
   Сообщение: Conflict: terminated by other getUpdates request
   Это означает, что другой экземпляр бота уже использует getUpdates с тем же токеном.
   Возможные причины:
   1. Бот запущен на другом сервере/машине с тем же токеном
   2. Бот был запущен ранее и не был корректно остановлен
   3. Бот запущен локально (вне Docker) одновременно с Docker контейнером
   4. Контейнер был перезапущен, но старый процесс polling все еще работает
   Решение: Убедитесь, что только один экземпляр бота запущен с данным токеном.
```

## Ручная диагностика

### Шаг 1: Проверка запущенных контейнеров

Проверьте, нет ли нескольких контейнеров с ботом:

```powershell
docker ps -a --filter "name=bot" --format "table {{.Names}}\t{{.Status}}\t{{.Image}}"
```

Должен быть только один контейнер `dark-maximus-bot` в статусе `Up`.

### Шаг 2: Проверка логов

Проверьте логи контейнера на наличие сообщений о запуске/остановке бота:

```powershell
docker logs dark-maximus-bot --tail 100 | Select-String -Pattern "ShopBot|запуск|start|polling|Conflict|ERROR"
```

Ищите:
- Сообщения о запуске бота (`🟢 ShopBot: Запуск бота...`)
- Сообщения о конфликте (`TelegramConflictError`)
- Сообщения о webhook (`Webhook`)

### Шаг 3: Проверка локальных процессов Python

Проверьте, нет ли локальных процессов Python, запускающих бота:

```powershell
Get-Process python -ErrorAction SilentlyContinue | Select-Object Id, ProcessName, Path, StartTime
```

Если найдены процессы Python вне Docker контейнера, остановите их.

### Шаг 4: Проверка состояния webhook через Telegram API

Используйте Telegram Bot API для проверки состояния webhook:

```powershell
# Замените YOUR_BOT_TOKEN на токен вашего бота
$token = "YOUR_BOT_TOKEN"
Invoke-RestMethod -Uri "https://api.telegram.org/bot$token/getWebhookInfo" -Method Get
```

Если `url` не пустой, webhook установлен и его нужно удалить перед использованием polling.

## Решение проблемы

### Решение 1: Остановить все экземпляры бота

1. **Остановите Docker контейнер**:
   ```powershell
   docker compose stop bot
   ```

2. **Проверьте, что контейнер остановлен**:
   ```powershell
   docker ps -a --filter "name=dark-maximus-bot"
   ```

3. **Подождите 10-15 секунд** для завершения всех процессов

4. **Запустите контейнер заново**:
   ```powershell
   docker compose start bot
   ```

### Решение 2: Удалить webhook вручную

Если webhook установлен, удалите его через Telegram Bot API:

```powershell
# Замените YOUR_BOT_TOKEN на токен вашего бота
$token = "YOUR_BOT_TOKEN"
Invoke-RestMethod -Uri "https://api.telegram.org/bot$token/deleteWebhook" -Method Post
```

Или используйте curl:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/deleteWebhook"
```

### Решение 3: Проверить другие серверы/машины

Если бот должен работать только на одном сервере:

1. Проверьте все серверы, где может быть запущен бот
2. Убедитесь, что только на одном сервере запущен контейнер с ботом
3. Остановите бота на всех остальных серверах

### Решение 4: Перезапустить контейнер

Если проблема возникла после перезапуска контейнера:

```powershell
docker compose restart bot
```

Проверьте логи после перезапуска:

```powershell
docker logs dark-maximus-bot --tail 50 -f
```

## Предотвращение проблемы

### Рекомендации

1. **Используйте только один экземпляр бота** с каждым токеном
2. **Не запускайте бота локально**, если он уже запущен в Docker
3. **Корректно останавливайте бота** перед перезапуском контейнера
4. **Не устанавливайте webhook**, если используете polling (и наоборот)
5. **Мониторьте логи** на наличие предупреждений о конфликтах

### Автоматическая защита

Система автоматически:
- Проверяет наличие webhook перед запуском polling
- Удаляет webhook, если он установлен
- Логирует подробную информацию о конфликтах
- Помечает бота как не запущенного при обнаружении конфликта

## Связанные документы

- [Документация по развертыванию](deployment/docker-deployment.md)
- [Руководство по админ-панели](admin/admin-panel-guide.md)
- [Документация aiogram](https://docs.aiogram.dev/en/v3.21.0/)

## Технические детали

### Код обработки конфликта

Конфликт обрабатывается в `src/shop_bot/bot_controller.py`:

- Функция `_ensure_no_webhook()` проверяет и удаляет webhook перед запуском polling
- Метод `_start_polling()` обрабатывает `TelegramConflictError` с подробным логированием
- При обнаружении конфликта бот помечается как не запущенный

### Telegram Bot API ограничения

Согласно документации Telegram Bot API:
- Метод `getUpdates` не может использоваться одновременно несколькими экземплярами бота
- Webhook и polling не могут работать одновременно
- При установке webhook метод `getUpdates` перестает работать

## Поддержка

Если проблема не решается после выполнения всех шагов:

1. Соберите логи контейнера:
   ```powershell
   docker logs dark-maximus-bot > bot-logs.txt
   ```

2. Проверьте состояние webhook:
   ```powershell
   # Замените YOUR_BOT_TOKEN на токен вашего бота
   $token = "YOUR_BOT_TOKEN"
   Invoke-RestMethod -Uri "https://api.telegram.org/bot$token/getWebhookInfo" | ConvertTo-Json
   ```

3. Создайте issue в репозитории с приложенными логами и результатами проверок

