# Режимы предоставления ключей и подписок

**Последнее обновление:** 26.11.2025

## Описание

В системе реализована возможность выбора режима предоставления VPN доступа пользователям при покупке тарифа. Администратор может настроить для каждого тарифа один из режимов:

1. **Ключ** (по умолчанию) - пользователь получает только VLESS ключ
2. **Подписка** - пользователь получает только URL подписки
3. **Ключ + Подписка** - пользователь получает и ключ, и подписку
4. **Личный кабинет** - пользователь получает доступ к личному кабинету с уникальным токеном
5. **Личный кабинет + Подписка** - пользователь получает доступ к личному кабинету и URL подписки

## Настройка режима

### В веб-панели

1. Перейдите в раздел **Настройки → Серверы**
2. Выберите хост и нажмите **Добавить тариф** или откройте существующий тариф для редактирования
3. В поле **"Режим предоставления"** выберите один из вариантов:
   - Ключ
   - Подписка
   - Ключ + Подписка
   - Личный кабинет
   - Личный кабинет + Подписка
4. Сохраните изменения

### В базе данных

Режим хранится в поле `key_provision_mode` таблицы `plans`:
- `'key'` - только ключ (default)
- `'subscription'` - только подписка
- `'both'` - ключ и подписка
- `'cabinet'` - только личный кабинет
- `'cabinet_subscription'` - личный кабинет и подписка

## Кнопки в клавиатуре управления ключом

В клавиатуре управления ключом (`create_key_info_keyboard`) отображаются следующие кнопки в зависимости от настроек:

### Кнопка "⚙️ Настройка"

Кнопка для перехода к инструкциям по настройке VPN.

**Условия отображения:**
- Кнопка отображается **всегда** для всех ключей
- **Не зависит** от настройки "Тип сервера" (production/development)
- **Не зависит** от настройки `codex_docs_domain`
- Всегда ведёт на фиксированный URL: `https://help.dark-maximus.com/setup`

**Расположение кнопки:**
- Отображается после кнопки "🗂️ Личный кабинет" (если она есть)
- В одном ряду с кнопкой "🔑 Подписка" (если она есть)

### Кнопка "🗂️ Личный кабинет"

Для ключей с режимами предоставления "Личный кабинет" или "Личный кабинет + Подписка" в клавиатуре управления ключом отображается кнопка "🗂️ Личный кабинет".

**Условия отображения:**
- Кнопка отображается **только** в режиме `production` (настройка "Тип сервера" = "production")
- Кнопка отображается **только** для ключей с `provision_mode = 'cabinet'` или `'cabinet_subscription'`
- В режиме `development` кнопка не отображается, так как Telegram требует HTTPS для Web App URL, а localhost не поддерживается
- Для отображения кнопки необходимо также настроить домен личного кабинета (`user_cabinet_domain`) в настройках

**Расположение кнопки:**
- Кнопка "🗂️ Личный кабинет" отображается **первой** (над кнопками "⚙️ Настройка" и "🔑 Подписка")
- Кнопка открывает Web App с URL личного кабинета и уникальным токеном доступа

### Кнопка "🔑 Подписка"

Для ключей с режимами предоставления "Подписка", "Ключ + Подписка" или "Личный кабинет + Подписка" отображается кнопка "🔑 Подписка".

**Условия отображения:**
- Кнопка отображается **всегда**, если у ключа есть `subscription_link`
- **Не зависит** от настройки "Тип сервера"
- Отображается для режимов: `subscription`, `both`, `cabinet_subscription`

**Расположение кнопки:**
- В одном ряду с кнопкой "⚙️ Настройка" (если нет кнопки "🗂️ Личный кабинет")
- После кнопки "🗂️ Личный кабинет" (если она есть)

## Технические детали

### API 3x-ui

Для получения subscription link используется следующая логика:

1. При создании клиента в 3x-ui автоматически генерируется `subId`
2. Система получает настройки подписки с панели через API `/panel/setting/all`
3. Формируется subscription link: `{subURI}{subId}`

### Пример subscription link

```
https://serv1.dark-maximus.com/subs/h6ld5dncl3vx8izf
```

### Обработка ошибок

Если не удается получить subscription link (например, не настроен subURI на панели 3x-ui), система автоматически переключается на режим "только ключ" и логирует предупреждение.

## Преимущества подписки

- **Автоматическое обновление** - при изменении конфигурации сервера пользователю не нужно обновлять ключ вручную
- **Удобство** - достаточно один раз добавить подписку в VPN приложение
- **Множественные серверы** - подписка может содержать несколько серверов

## Использование в коде

### Получение subscription link

```python
from shop_bot.modules import xui_api

subscription_link = await xui_api.get_client_subscription_link(
    host_name="serv1",
    client_email="user123-key1@serv1.bot"
)
```

### Отправка сообщения с ключом/подпиской

```python
from shop_bot.config import get_purchase_success_text

final_text = get_purchase_success_text(
    action="создан",
    key_number=1,
    expiry_date=datetime.now(),
    connection_string="vless://...",  # Опционально
    subscription_link="https://...",   # Опционально
    provision_mode="both",             # 'key', 'subscription', 'both', 'cabinet', 'cabinet_subscription'
    user_id=123456,                    # Обязательно для режимов cabinet/cabinet_subscription
    key_id=789                         # Обязательно для режимов cabinet/cabinet_subscription
)
```

### Формирование клавиатуры с кнопками

```python
from shop_bot.bot.keyboards import create_key_info_keyboard

# Кнопка "⚙️ Настройка" добавляется ВСЕГДА
# Кнопка "🗂️ Личный кабинет" добавляется если:
#   - server_environment = "production"
#   - provision_mode ключа = "cabinet" или "cabinet_subscription"
#   - user_cabinet_domain настроен
# Кнопка "🔑 Подписка" добавляется если:
#   - есть subscription_link

keyboard = create_key_info_keyboard(
    key_id=789,
    subscription_link="https://...",  # Опционально
    key_auto_renewal_enabled=True     # Опционально
)
```

**Примеры расположения кнопок:**

1. **Режим "key" (только ключ):**
   ```
   [⚙️ Настройка]
   [🔁 Продлить этот ключ]
   [🔄 Автопродление: 🟢Вкл]
   [⬅️ Назад к списку ключей]
   ```

2. **Режим "both" (ключ + подписка):**
   ```
   [⚙️ Настройка] [🔑 Подписка]
   [🔁 Продлить этот ключ]
   [🔄 Автопродление: 🟢Вкл]
   [⬅️ Назад к списку ключей]
   ```

3. **Режим "cabinet" (личный кабинет) в production:**
   ```
   [🗂️ Личный кабинет]
   [⚙️ Настройка]
   [🔁 Продлить этот ключ]
   [🔄 Автопродление: 🟢Вкл]
   [⬅️ Назад к списку ключей]
   ```

4. **Режим "cabinet_subscription" (личный кабинет + подписка) в production:**
   ```
   [🗂️ Личный кабинет]
   [⚙️ Настройка] [🔑 Подписка]
   [🔁 Продлить этот ключ]
   [🔄 Автопродление: 🟢Вкл]
   [⬅️ Назад к списку ключей]
   ```

## Миграция существующих тарифов

При обновлении системы все существующие тарифы автоматически получают режим `'key'` (только ключ), что соответствует текущему поведению.

## См. также

- [Архитектура системы](../architecture/architecture-rules.md)
- [Работа с 3x-ui API](modules.md)
- [База данных](database.md)

